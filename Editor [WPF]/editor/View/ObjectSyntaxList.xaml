<UserControl x:Class="editor.View.ObjectSyntaxList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <ListView Margin="10" Name="lvDataBinding">
        <ListView.ItemTemplate>
            <DataTemplate>
                <Canvas Width="250" Height="250" Background="Aquamarine">
                    <TextBlock Canvas.Left="20" FontWeight="Bold">ObjectType</TextBlock>
                    <TextBlock Text="{Binding ObjectType}" FontWeight="Bold" />
                </Canvas>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
